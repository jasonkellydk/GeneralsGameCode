#pragma once

// Shared only by the private DX11 subsystem translation units. Native API
// types and ownership remain below Backend/dx11; WW3D2 uses RenderBackendTypes.
#include "DX11BackendState.h"
